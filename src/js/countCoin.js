const countCoin = (coins, score, scoreText) => {
  console.log('coins', coins)
  coins.disableBody(true, true);
  // score =+ 50;
  // scoreText.changeText = 'score' + '#{score}'

  // return score;
}

export default countCoin;